# Progress & handoff — AW File Storage

Living status doc for session continuity. Read this + [AGENTS.md](../AGENTS.md) +
the specs in [docs/specs/](specs/0001-secure-file-storage-platform/index.md)
before continuing. Last updated: **2026-09-02**.

## TL;DR — where we are

**The app is BUILT and DEPLOYED to production, and the full product loop works.**
Drop a `.vcf` in the browser → it uploads directly to R2 → validates → publishes
→ resolves at a live public URL. Non-vCard files stay private with signed
download links. Auth, tenancy, uploads, and publishing are all done and verified.

- **Live app:** https://www.awvcard.com (custom domain; `awvcard.com` 301→www). Old `*.workers.dev` still resolves.
- **Owner account:** `jclark@americaworks.com` (prod). Local dev owner: `owner@americaworks.test`.
- **Public vCard URLs:** `https://contacts.awvcard.com/c/First_Last.vcf` (download) + `https://contacts.awvcard.com/c/First_Last` (styled landing page). Since Phase 11, `contacts.awvcard.com` is served by the **app Worker** (not the R2 bucket directly); `PUBLIC_FILE_DOMAIN` still names it. Slug is `First_Last`, case-preserved (Phase 6).
- **State:** everything through **Phase 14** (spec 0011, O365 cert auth) is **committed + deployed on `main`**. Latest PRs: #30/#31 (public landing + analytics), #35 (host-based access + Site toggle), #36 (Office 365 sync), #37 (O365 cert client-assertion auth). Cron worker (`aw-file-storage-cron`, `0 3 * * *` UTC) runs the nightly O365 reconcile — **redeployed 2026-09-03** (the spec-0010 redeploy was documented but never actually happened, so the deployed worker lacked the `/api/cron/o365-sync` call and the nightly reconcile silently ran cleanup-only until 2026-09-03).
- **⚠️ The cron worker deploys SEPARATELY.** `npm run deploy` builds + deploys only the **app** worker (`aw-file-storage`). Any change under `cron/` needs its own `npx wrangler deploy --config cron/wrangler.jsonc` — forgetting this is what left the nightly O365 reconcile un-deployed for a day.
- **Migrations 0010–0013 applied (local + remote):** 0010 `card_stat_daily`, 0011 `app_settings`, 0012 `file.o365_*` columns, 0013 `app_settings.o365_sync_enabled`. All additive. **Lesson relearned:** apply migrations to `--remote` BEFORE `npm run deploy` — deploying code that reads a new column before the migration lands 500s the page (hit twice this session, on Files).
- **⚠️ Deploy/migration hazard:** a Drizzle table-rebuild on a Better Auth **parent** table cascade-wiped `member`/`file`/`audit` on D1 (see gotcha #9). Both local and prod `member` tables were restored by hand. **Inspect every migration's SQL before applying.**

## What's done (phase by phase, all verified)

- **Phase 0 — scaffold:** Next.js 16 (App Router) on Cloudflare Workers via OpenNext, Tailwind v4, TypeScript.
- **Phase 1 — tenancy (spec 0002):** Drizzle schema on D1, org-isolation wrapper (`src/server/org-db.ts`), isolation test suite (`test/isolation.test.ts`, 5 tests).
- **Phase 2 — auth (spec 0001):** Better Auth (self-hosted, **pinned to 1.4.21**) with sessions (8h rolling + 7d cap), active-org on sign-in, CSRF/origin proxy, per-account lockout, HaveIBeenPwned breach check, Resend email (console fallback), organization invitations, and 2FA (TOTP + backup codes, required for owner/admin).
- **Phase 3 — uploads (spec 0003):** presigned direct-to-R2 upload (`aws4fetch`), server-side finalize, vCard validation + normalization + publish to the public bucket, slug derivation + collision, non-vCard move to a private key, unpublish/delete, private download links, `listFiles`, and scheduled-cleanup logic + endpoint. **Republish (AC-10) was intentionally dropped** — see decisions.
- **Phase 4a — app shell:** header/nav/rail/footer to the mock, `(app)` route group with auth redirect. **Committed.**
- **Phase 4b — Upload Center:** interactive drop zone + client upload queue (concurrency 3, progress, badges, retry, copy-link), wired to the API. Duplicate-content uploads handled gracefully (409). **Committed.**
- **Phase 4c — live rail + file list:** `getRailData` (`src/server/rail.ts`) + `GET /api/rail` feed a client `SideRail` with live Storage Usage, today's Upload History, and role-scoped Recent Activity (org-wide for owner/admin, own-only for members). New `FileManager` lists the org's files (`GET /api/files`) with copy-link/download/unpublish/delete, role-gated by a server-computed `canManage`. A shared `AppDataProvider` context lets a settled upload refresh the rail + list with no page reload (AC-10). Unpublish + delete **verified in-browser.**
- **Phase 4d — dashboard, a11y, responsive, org switcher:** Dashboard rebuilt to `docs/Designs/mock-dashboard.jpg` — stat cards (team members / storage / published cards), an uploads-per-day area chart + a file-types donut (dependency-free inline SVG in `src/components/charts.tsx`), and a recent-activity feed, all from real org data via `getDashboardData` (`src/server/dashboard.ts`). No invented metrics. Org switcher in the header user menu (shown when the user is in >1 org): `getShellData` returns the caller's orgs; selecting one calls `authClient.organization.setActive` then hard-reloads so every panel reflects the new org (AC-17). Accessibility (AC-14/AC-18): the Upload Center has one polite `aria-live` region that announces start/half-way/finish/failure only, progress bars carry `role="progressbar"` + values, and a global `prefers-reduced-motion` rule neutralises motion. Responsive (AC-15): new `AppShellBody` makes the rail an off-canvas drawer behind a "Panels" control below `lg` (Esc/backdrop to close), and file rows stack + action buttons wrap so the layout holds at 360px.
- **Phase 5 — user management (spec 0005):** the Members section in `/settings` plus the missing self-service pages. Built and verified in local dev; **not yet committed or deployed.**
  - **Slice 1:** `member.status` (`active`/`suspended`) via the org plugin's `additionalFields` + migration `0004`.
  - **Prerequisite:** `requireApiRole(minRole)` in `session.ts` returns typed 401/403 for route handlers (the old `requireOrgRole` threw → 500); `requireOrgRole` now `notFound()`s on insufficient role.
  - **Slice 2:** `/accept-invitation/[id]` repaired — `previewInvite` (never leaks the email on a bad id), account create → browser sign-in → into the app, `member.joined` audit. Reuses the existing `/api/invitations/accept`.
  - **Slice 3:** roster — `listMembers` (explicit org filter, cursor pagination) + `GET /api/members` + `MembersSection` (search/filter/states, matches `mock-user-management.jpg`).
  - **Slice 4:** invitations UI — `GET /api/invitations`, invite form, `DELETE .../[id]` (revoke), `POST .../[id]/resend` (rate-limited 429), all audited (`InvitationsPanel`).
  - **Slice 5:** role change + removal — `PATCH`/`DELETE /api/members/[id]`, last-active-owner (AC-5) + self-action (AC-6) guards, inline role dropdown + Remove confirm, audited.
  - **Slice 6:** suspension — `setMemberStatus` (revokes sessions in the same action), sign-in refusal in the auth `before` hook, session-create hook prefers an active membership, Suspend/Reactivate UI.
  - **Slice 7:** detail page `/settings/users/[id]` — sign-in state, storage footprint, 2FA status; **Revoke sessions** + **Reset two-factor** actions (`getMemberDetail`, `member-actions.tsx`).
  - **Slice 8:** self-service `/forgot-password` + `/reset-password` (shared `AuthShell`) + "Forgot password?" link on sign-in.
  - **Slice 9:** `test/members.test.ts` — guards, org isolation, session revocation, audit rows.
  - **Scope extensions (beyond spec 0005, by request):** admin **Set password** + **Send reset link** on the detail page (`adminSetPassword`, `sendMemberResetLink`; the link is returned so it works without Resend), and self-service **name** editing (`ProfileSection`, Better Auth `updateUser`). Spec 0005 deliberately chose self-service-only; these override that — record in the spec's follow-up when reconciling.
- **Phase 6 — create-card form (spec 0006):** a **Create Card** nav tab → `/create-card` with a 3-step wizard (Name & contact → Work → Address) that builds a vCard 3.0 in the browser (`src/lib/vcard-builder.ts`, a TS port of `docs/Designs/vcf Generator.py` + RFC 6350 escaping) and publishes it through the existing upload pipeline, showing the public URL. Niceties: auto full-name from first+last, Title-case on blur (names/org/city etc., not email/phone/state/zip), Country defaults to "USA" (a lone country emits no `ADR`), Enter never publishes (button-only), required-field guard on step 1. Tests in `test/vcard-builder.test.ts`. **Slug format revised:** `deriveSlug` now yields `First_Last` (case-preserved, underscore) instead of lowercase-hyphenated; collision suffix is `_XXXXX`. **`r2Put` fix:** server-side R2 writes now go via a presigned URL + plain fetch (aws4fetch's signed body dropped Content-Length under the Node dev runtime → R2 411). Built + verified in local dev; **not committed/deployed.**
- **Phase 7 — files view (spec 0007):** a dedicated **Files** nav tab → `/files` (`FilesView`) matching `docs/Designs/mock-files.jpg`: a table (select, Name + type badge, Size, Modified, Uploaded By, Status) with **search** + **filter by type** (`src/lib/file-type.ts`), a count, per-row **`⋯` dropdown** (Copy link / Download / Rename / Unpublish / Delete) rendered via a **portal** (escapes the table's scroll container), **custom confirm dialogs** (`confirm-dialog.tsx`) for delete/unpublish + bulk delete, and inline **rename** (`PATCH /api/files/[id]` → `renameFile`, audited `file.renamed`). `listFiles` enriched with uploader name / content type / updated-at. Header search box removed (Files page owns search); the old inline `FileManager` deleted. **New app logo + favicon:** `src/components/logo.tsx` (contact-card-with-broadcast mark, brand blues) + `src/app/icon.svg`, used in the header and the signed-out pages. **Folders deferred.** Tests: `test/file-type.test.ts`. Built + verified in local dev; **not committed/deployed.**
- **Phase 8 — two-factor (spec 0001 AC-11, completed + extended):** closes the deferred **sign-in 2FA step** — after correct credentials, if `signIn.email` returns `twoFactorRedirect`, the card shows a code step (TOTP verify + "use a backup code"). **Settings → Two-factor** section (enable → `/enroll-2fa`, disable with password); the enrol page is restyled into `AuthShell` with a scannable **QR** (works with Google Authenticator; needs the `qrcode` dep). **Admin mandatory 2FA (new):** a `twoFactorRequired` flag on `member` (additionalFields → migration); the member detail page has a **Require 2FA** toggle (green when the account is enrolled) + a "Two-factor required" row; the `(app)` layout redirects a required-but-not-enrolled user to `/enroll-2fa` on every route (`twoFactorEnrollmentRequired`), and the self-service **Disable** is hidden when required. All member-action confirmations moved from `window.confirm` to the modal `ConfirmDialog`. Password hashing for admin-set + invite re-onboard switched to `hashPassword` from `better-auth/crypto` (the `$context.password.hash` path threw "No auth context"). **Invitation re-onboard fix:** `acceptInvite` now re-adds a previously-removed (orphaned) user instead of failing "already exists" — sets a new password on the surviving `user` row and re-creates the membership; never touches a user still active elsewhere. **Needs the `qrcode` install + the `member.twoFactorRequired` migration** before typecheck. Built + verified in local dev.
- **Phase 8 — two-factor (spec 0001 AC-11):** committed + deployed. Sign-in 2FA code step (TOTP + backup), Settings Two-factor enable/disable, enrol page with QR, and admin **per-member mandatory 2FA** (`member.twoFactorRequired`; `(app)` layout redirects required-but-not-enrolled users to `/enroll-2fa`; Disable hidden when required). Also: member-action confirms → modal `ConfirmDialog`; password hashing via `hashPassword` from `better-auth/crypto`; `acceptInvite` re-onboards a previously-removed user instead of "already exists".

- **Phase 9 — printable signature + QR (DONE — committed + deployed).** Goal: from a **published vCard**, generate an America Works **signature card** that looks like `docs/Designs/` (the emailed-signature screenshot the engineer provided): left = AW logo + `americaworks.com` / `Employers | Job Seekers`; a red divider; right = name, title, phones, email, address, "Schedule a meeting" + social icons; a **QR of the public `contacts.awvcard.com/c/<slug>.vcf` URL** underneath; a confidentiality disclaimer footer.
  - **Decisions (made):** output = **BOTH** a print/PDF page AND a **Copy email signature** button. Scope = **any published vCard**, action lives on the Files rows + the Create-Card success screen. **No DB change** — parse the stored `.vcf` on demand (it's the source of truth). AW branding lives in one editable config file with placeholder URLs.
  - **SHIPPED (committed + deployed):**
    1. `parseVcard(raw)` + `formatPhoneDisplay(raw)` in `src/server/vcard.ts` — unfolds continuation lines, unescapes RFC 6350, classifies `TEL` by TYPE (fax → cell/mobile → work → spare), splits + pre-joins `ADR`, derives name from `N` or `FN`. Handles vCard 2.1 bare type params. Never throws. Tests: `test/vcard-parse.test.ts` (12, passing).
    2. `src/server/signature.ts` — `getCardForSignature(env, {orgId}, id)` (auth'd, org-scoped via `orgDb`; reads the durable private `.vcf` with `r2GetText`, returns parsed fields + public URL) and `resolvePublishedCardUrl(env, id)` (public, slug-only, for the QR endpoint). Exported `publicUrlFor` from `uploads.ts` for reuse.
    3. `GET /api/cards/[id]/qr` (`src/app/api/cards/[id]/qr/route.ts`) — **PUBLIC/unauthenticated on purpose** (email recipients fetch with no cookie; it only encodes the already-public URL, 404s for non-published ids). Emits **PNG** via `QRCode.toDataURL` → byte decode, **falls back to SVG** (`toString`) if PNG generation throws in the Worker runtime. `Cache-Control: public, max-age=3600`.
    4. `/signature/[id]` (`src/app/(app)/signature/[id]/page.tsx`, **inside the app shell** — header + nav + side rail) → `SignatureView` (`src/components/signature-view.tsx`): renders the HTML as a WYSIWYG preview with a primary **Print / Save as PDF** (`window.print()`) + Copy public link. (The earlier "Copy email signature" clipboard button was **removed** by request — the page is now a print/PDF tool; `buildSignatureText` deleted with it.) Absolute base for logo/QR from `APP_URL` (fallback: request host). **Print cleanliness:** app chrome carries `print:hidden` (header+nav wrapper + footer in `(app)/layout.tsx`; rail + Panels bar + `main`→white in `app-shell-body.tsx`); `globals.css` `@media print` sets `@page{margin:0}` (drops the browser's title/URL/date header-footer), pads the body 14mm, whitens the ground, and forces `print-color-adjust:exact` so the **red divider + blue Schedule button backgrounds actually print**.
    5. `src/lib/signature-brand.ts` (AW config: links/**per-state socials**/colours/disclaimer) + `src/lib/signature-html.ts` (`buildSignatureHtml` — nested-table, inline-styled, absolute-URL markup, rebuilt to match the real Outlook signature: full-height red divider, bold-navy name/title, `telephone:`/`mobile:`/`email:` blue-underlined values with `x117` ext, two-line address, blue Schedule-a-meeting button + **round** social icons inline, QR under the details, confidentiality disclaimer footer).
    - **Per-state socials:** `socialsByState` keyed by 2-letter abbrev + org-wide `socials` fallback; `normalizeState()` maps "New York"/"NY"/"ny" → "NY" (full US map included for future states); `socialsForState(brand, card.address.state)` picks the set from the card's address. **NY is wired** (real FB/X/IG accounts); add states by dropping an entry into `socialsByState`. Tests: `test/signature-brand.test.ts`.
    - **Social icons:** real hosted PNGs in **`public/social/{facebook,x,instagram,linkedin}.png`** (generated from authored SVGs via `sharp`, 44px for 22px display), referenced by absolute URL so they render in email. Regenerate with the one-off sharp script if the marks change. LinkedIn only shows when a LinkedIn URL is set (none for NY yet).
    - **Entry points:** Files row `⋯` menu gains **Signature** (published vCards only, `MenuLink`); Create-Card success screen gains **Create signature** (captures `fileId` from finalize).
  - **Still needs from engineer (placeholders in `signature-brand.ts` until then):** real URLs for `americaworks.com`, **Employers**, **Job Seekers**, **"Schedule a meeting"** (one org-wide link — per-person isn't in the vCard). Disclaimer = generic confidentiality paragraph (replace with AW's exact text).
  - **Regional social links — now admin-managed (branch `regional-social-links`).** Per-state social URLs moved out of `signature-brand.ts` into a DB table `org_social_link` (migration `0006`, one row per org+state; `"*"` = org-wide default), editable in **Settings → Regional social links** (owner/admin): `GET/PUT /api/social-links`, `DELETE /api/social-links/[state]`, `POST /api/social-links/seed` (imports the built-in NY + default so they're editable). Resolution (`resolveSocials`, `src/server/social-links.ts`): exact-state row → org `"*"` row → built-in `signature-brand.ts` fallback. The signature page resolves per the card's state and passes `socials` into `buildSignatureHtml` (which no longer calls `socialsForState`). URL fields are pick-or-type (datalist of stored URLs). Tests: `test/social-links.test.ts`. **Needs migration `0006` applied (local + prod).**
  - **Caveats/notes:** email-signature images MUST be public HTTPS URLs — logo `/aw-logo.png`, QR via the endpoint. Print QR + preview use the same hosted endpoint (not a data-URL). PNG path verified in Node; SVG fallback in place if the Worker runtime can't do pngjs/zlib — **watch for this on first deploy** (a broken PNG would still return valid SVG). `qrcode` server-side use is new (was client-only in Phase 8) — confirm it bundles under OpenNext at `npx opennextjs-cloudflare build`.

- **Phase 10 — Files scale-up: server-side pagination, search & filtering (spec 0007 follow-up — committed + deployed, PR #25).** The Files page no longer loads every row and filters in the browser. It's now a properly queryable server dataset: **keyset (cursor) pagination** — opaque `[sortValue, id]` cursor, no OFFSET, stable under concurrent inserts/deletes — with a **Load more** button and a server-rendered first page (default **30 rows**). Search, type/status filter, and Name/Size/Modified sorts all run in SQL and sync to the URL (shareable, back-button correct). To make **contact content searchable** (find by company/name, not just filename), the vCard's fields are denormalised into new `file` columns `contact_name/org/title/email` + a persisted `category` (additive **migration 0008** — five `ADD COLUMN` + six composite indexes, no table rebuild), populated at finalize (reusing `parseVcard`/`fileCategory`). Existing rows are filled by an idempotent **owner-only** `POST /api/admin/backfill-search` (run once per org after deploy; removable after). New server layer: `listFilesPage()` + `encodeCursor`/`decodeCursor` in `uploads.ts`; `GET /api/files` now takes `q/category/kind/status/sort/dir/cursor/limit`. FTS5 is a documented future upgrade over the same columns if an org exceeds ~10k cards. Tests: `test/files-list.test.ts` (10, incl. the keyset-vs-offset stability case).
- **Phase 10 — Edit a published contact card in place (spec 0006 follow-up — committed + deployed, PR #26).** Published vCard rows gain an **Edit card** action (`/files/[id]/edit`) that reuses the Create-Card wizard, **pre-filled** from the stored `.vcf` (via `getCardForSignature` + `cardFieldsFromParsed`), and **republishes under the SAME slug** — so the public URL and any printed QR keep working. `editVcard()` (`uploads.ts`) re-validates, overwrites both the private copy and the public object at the existing slug, refreshes the `contact_*` search fields, adjusts org storage usage by the size delta, rejects a checksum collision with a *different* card, and audits `vcard.edited`. New `PATCH /api/files/[id]/card`; `canManage` re-checked server-side. **No DB migration** (reuses existing columns). Round-trip tests in `test/vcard-builder.test.ts` (parse → map → build → parse is stable). **Note:** the slug is intentionally NOT re-derived on edit, so over time a URL can drift from a renamed contact; delete + re-create is the path to a fresh URL.

- **Create Card refinements (spec 0006 follow-up — committed + deployed, PR #28).** On the Work step, **Organization is now a fixed dropdown** of the AW regional entities (values in `ORG_OPTIONS`, `create-card-form.tsx`) instead of free text — an off-list value on an older card being edited is preserved as an extra option so it's never silently dropped, and a blank entry keeps the field optional. **Website is read-only**, always `https://www.americaworks.com` (`FIXED_WEBSITE`), applied in both create and edit.
- **State dropdown + city/state search (spec 0006/0007 follow-up — committed + deployed, PR #29).** Create Card's **State** is now a dropdown (`US_STATE_OPTIONS`) storing the canonical 2-letter abbreviation (normalised on edit-load), which also feeds the per-state signature features cleanly. Files search now covers **city and state**: a `file.contact_location` column (**additive migration 0009, applied local + remote**) holds a searchable `"City Abbr FullName"` blob (e.g. `"Bronx NY New York"`) built by `buildLocationText` (`signature-brand.ts`), so one search matches Bronx, NY, *or* New York. Populated at publish/edit; `backfillSearchFields` extended to fill it (re-fills rows that predate the column) — **the prod backfill was re-run** post-deploy. Added to the `listFilesPage` search `OR`. Also shipped in #29: the org dropdown now uses **full legal names** ("America Works of New York, Inc", …), and the Files row action menu **opens upward** when it would clip at the viewport bottom (`files-view.tsx`). Tests: `buildLocationText` in `test/signature-brand.test.ts`; location search in `test/files-list.test.ts`.

- **Phase 11 — Public card landing pages + view/scan/download analytics (spec 0008 — committed + deployed, PR #30 + #31).** Published cards now serve a **styled public landing page** at `contacts.awvcard.com/c/<slug>` (self-contained inline-styled HTML: Add-to-contacts, tap-to-call, email, address+map, per-state socials + admin logo, QR) alongside the unchanged `/c/<slug>.vcf`. To render HTML and count hits, `contacts.awvcard.com` was **moved off the R2 bucket onto the app Worker** (the cutover: remove the R2 custom domain first, then deploy the Worker route — it's in `wrangler.jsonc`). A **new `/logos/[org]/[file]` passthrough** serves the admin-uploaded state logos from R2 (they used to come straight from the bucket). **Engagement counts** live in a D1 daily rollup `card_stat_daily` (**additive migration 0010**), incremented best-effort via `ctx.waitUntil` with bot/prefetch filtering; three metrics (landing = view, `?src=qr` = scan, `.vcf` = download). New server layer `src/server/card-stats.ts` + `src/lib/card-landing-html.ts` + `src/app/c/[slug]/route.ts`. In-app: an **Engagement column** on Files, a `GET /api/cards/[id]/stats` endpoint, and a **Dashboard "Card Engagement" + "Top Cards"** panel, all role-scoped. QR/signatures now encode the landing URL (`landingUrlFor`, `?src=qr`). Transparent `ca.png`/`pa.png` logos + `uploadStateLogo` now deletes the old R2 object on a file-type change (`docs/logo-assets.md`). Tests: `test/card-stats.test.ts`.
- **Phase 12 — Host-based card access + Site toggle (spec 0009 — committed + deployed, PR #35).** `/c/*` is **public only on `contacts.awvcard.com`**; on the app host (`www`) it **requires a login** (redirect to `/sign-in`) and **never counts**, so `www` is not a second public directory and staff previews don't inflate analytics. Counting is host-based (only the public host); the old `?preview=1` flag was removed. A **Settings → Site** toggle (owner/admin) turns the `www` login requirement on/off, backed by a single-row `app_settings` table (**additive migration 0011**). Also: `publicUrlFor`/`landingUrlFor` use `http` for a localhost `PUBLIC_FILE_DOMAIN` so **dev generates working local card links** (set `PUBLIC_FILE_DOMAIN="localhost:3000"` in `.dev.vars`). Files search no longer matches the **uploader name** (one admin uploading every card made a last-name search hit every row). Tests: `test/app-settings.test.ts`.
- **Phase 13 — Office 365 CustomAttribute1 sync (spec 0010 — committed + deployed, PR #36; live but toggle-gated).** Each published card's public `.vcf` URL is written into the matched staff member's Exchange **`CustomAttribute1`** (`onPremisesExtensionAttributes.extensionAttribute1`) via **Microsoft Graph, app-only (client credentials)**. Writes on publish/edit, clears on unpublish/delete, plus a **nightly reconcile** (the cron worker now also calls `POST /api/cron/o365-sync`). Idempotent (PATCH only on a diff), best-effort (never blocks a user action), matched by email = `mail`/`userPrincipalName`; 0 or many matches → `no_match`/`ambiguous`, all recorded. Server layer: `src/server/graph.ts` + `src/server/o365-sync.ts`; sync state in five **additive `o365_*` columns** on `file` (**migration 0012**). **Enable is a UI toggle** (`Settings → Office 365`, with "Sync all now" + a status summary), stored in `app_settings` (**migration 0013**); the `GRAPH_*` credentials are **Worker secrets** and gate the feature. **Cloud-only tenant** confirmed (extensionAttribute1 is read-only in Graph for on-prem-synced users). Entra app: `Office 365 VCard - awvcard.com`, `User.ReadWrite.All` + admin consent. **Verified in prod:** a new card's URL landed in CustomAttribute1. Tests: `test/o365-sync.test.ts`. **Follow-ups:** run "Sync all now" to backfill existing cards; rotate/replace the client secret before expiry (prefer a certificate); scope the app to an Administrative Unit.

- **Deploy:** live on Cloudflare **Workers Paid** plan (Free plan's 3 MiB limit was exceeded).

## Production setup (already done)

- **Remote D1 migrated** (`wrangler d1 migrations apply aw-file-storage --remote`).
- **Secrets set** (`wrangler secret put`): `BETTER_AUTH_SECRET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `CRON_SECRET`, `APP_URL` (= the workers.dev URL).
- **Vars in `wrangler.jsonc`:** `PUBLIC_FILE_DOMAIN`, `R2_ACCOUNT_ID`, `R2_PRIVATE_BUCKET`, `R2_PUBLIC_BUCKET`.
- **R2 CORS** set on `aw-files-private` for `http://localhost:3000` + the prod origin (`config/r2-cors.json`).
- **r2.dev public URL** enabled on `aw-files-public`.
- `RESEND_API_KEY` is **not** set in prod → reset/invite emails log to `wrangler tail` instead of sending (fine for now).
- Deploy command: `npm run deploy` (= `opennextjs-cloudflare build && deploy`).

## Load-bearing decisions & gotchas (don't relearn these the hard way)

1. **Better Auth pinned to 1.4.21** — the `@better-auth/cli` (schema generator) lags the runtime; keep them matched. Trade-off: no built-in 2FA-code lockout (build our own if needed). Regenerate schema after plugin/option changes: `npx @better-auth/cli@latest generate --config ./better-auth.config.ts --output ./src/server/db/auth-schema.ts -y` → `drizzle-kit generate` → `wrangler d1 migrations apply`.
2. **R2 via the S3 API (`aws4fetch`), not bindings** — remote R2 bindings need a workers.dev subdomain and broke `next dev`. `src/server/r2.ts` holds presign + server-side ops.
3. **Next 16 uses `proxy.ts` (export `proxy`), NOT `middleware.ts`.** Our CSRF/origin check is `src/proxy.ts`.
4. **`(app)` routes need `export const dynamic = "force-dynamic"`** (they read the per-request session) or the production build fails trying to prerender them.
5. **Workers Paid plan required** to deploy (bundle > 3 MiB).
6. **Dev-server orphans:** stopping the task wrapper leaves `next dev` holding port 3000. Kill by PID: `netstat -ano | grep :3000` → `taskkill //F //PID <pid>`. Always confirm the server is on 3000, not 3002.
7. **UI verification:** the user checks UI in their own browser and screenshots — don't drive claude-in-chrome for it.
8. **Custom domain — DONE.** Registered `awvcard.com` on Cloudflare. App = Worker custom domain `www.awvcard.com` (canonical `APP_URL`); apex `awvcard.com` 301→www via a Redirect Rule (needs a proxied placeholder A record `@ → 192.0.2.1`). vCards = R2 bucket `aw-files-public` custom domain `contacts.awvcard.com` (`PUBLIC_FILE_DOMAIN`). Sign-in needs the browser origin trusted: added a `TRUSTED_ORIGINS` var (comma-separated) that `auth.ts` merges into Better Auth `trustedOrigins`. AC-17 **DONE** — the `X-Robots-Tag: noindex, nofollow` Response Header Transform Rule is live on `contacts.awvcard.com` (verified against a published card); see [contacts-noindex.md](contacts-noindex.md).
9. **D1 migration cascade wipe (DANGEROUS).** D1 runs each migration in a transaction, where SQLite **ignores `PRAGMA foreign_keys=OFF`**. So a Drizzle table-**rebuild** (emitted when a column default/constraint changes — e.g. the org quota default) does `DROP TABLE parent`, which cascade-deletes every child row (`member`, `file`, `audit_event`, …). Migration `0004` silently wiped these on local AND remote. **Always `cat` the generated SQL before applying; if it `DROP TABLE`s a parent (`organization`/`user`), hand-edit it to `ALTER TABLE` in place.** Restore a wiped membership with the `INSERT INTO member … SELECT FROM user,organization WHERE NOT EXISTS(…)` one-liner.
10. **HTTP/2 host in `proxy.ts`:** browsers send the host as the `:authority` pseudo-header (no `Host` header), so `req.headers.get("host")` was null behind Cloudflare and the CSRF check 403'd every sign-in on the custom domain. Fixed via `TRUSTED_ORIGINS` + host fallback.

## KNOWN ISSUES

- ~~**The `proxy.ts` path-exemption for `/api/cron/` and `/api/admin/` does NOT fire on the Cloudflare runtime.**~~ **RESOLVED.** The CSRF check is now uniform — **all** mutating callers must send an `Origin` matching the host, machine callers included. The cron worker sends `Origin: <APP_URL>` (`cron/src/index.ts`); the `/api/admin/bootstrap` caller must include `-H "Origin: <APP_URL>"` on its `curl`. The dead path-exemption was **removed** from `proxy.ts` (branch `chore-remove-dead-proxy-exemption`).

## What's next (TODO, roughly in order)

1. ~~**Phase 9 — printable signature + QR**~~ **Done** — committed + deployed. Follow-ups: real AW URLs for website/Employers/Job Seekers/Schedule-a-meeting + non-NY state socials in `signature-brand.ts`; decide whether the confidentiality disclaimer prints (currently shown); smoke-test the QR endpoint in prod (confirm PNG, else SVG fallback).
2. ~~**Phase 5 / 6**~~ **Done** — merged + deployed. The two spec-0005 scope deviations (admin password reset; self-service name/email edit) still want recording in that spec's follow-up. Prod fixes that landed with Phase 6: `wrangler.jsonc` now declares the `www.awvcard.com` custom domain (a bare `wrangler deploy` had removed it once); prod secrets `RESEND_API_KEY` and the correct `R2_ACCESS_KEY_ID` (was a 36-char mis-paste; must be 32) / `R2_SECRET_ACCESS_KEY` were set — the bad R2 key made every presigned upload 4xx and surfaced as a bogus CORS error. Reminder: `.dev.vars` never deploys; prod secrets are set separately.
3. ~~**Phase 4c / 4d**~~ **Done.** Still open from spec 0004: ETA column (AC-5), upload table as a semantic `<table>`, leave-warning + offline pause/resume (AC-13).
4. ~~**Deferred auth UI** — the sign-in page's **2FA code-entry step**.~~ **Done** — `src/app/sign-in/page.tsx` has the full flow: TOTP + backup-code entry (`verifyTotp`/`verifyBackupCode`), "Enter the 6-digit code" prompt, Verify, and the "Use a backup code" toggle. Verified live in prod. accept-invitation + password-reset pages were built in Phase 5.
5. ~~**Cron worker deploy**~~ **Done + deployed.** `cron/src/index.ts` sends `Origin: <APP_URL>` (passes the app's CSRF guard); `cron/wrangler.jsonc` `APP_URL` = `https://www.awvcard.com`. `aw-file-storage-cron` deployed with `CRON_SECRET` set and schedule `0 3 * * *` UTC. Verified live: `POST /api/cron/cleanup` (bearer + Origin) → `{"ok":true,"sweptSessions":4}`. (Follow-up done: the dead `/api/cron/` + `/api/admin/` proxy exemption was removed — CSRF is now uniform.)
6. ~~`X-Robots-Tag: noindex` transform rule on `contacts.awvcard.com` (AC-17)~~ **Done** — Cloudflare Response Header Transform Rule live + verified (`x-robots-tag: noindex, nofollow`); rule spec in [contacts-noindex.md](contacts-noindex.md). **Multipart uploads** > 90 MB (deferred).
7. **Reconcile spec 0005** — record the two accepted deviations (admin password reset; self-service name/email edit) in its follow-up.
8. ~~**`acceptInvite` atomicity** (D1 batch) refinement.~~ **Done** — the access-control trio (membership insert + invitation→accepted + `member.joined` audit) now commits in one `db.batch()` (implicit transaction). Better Auth's user creation stays separate (can't join the batch); the re-onboard guards keep retries idempotent. Tests: `test/invitations.test.ts` (happy path, already-member idempotence, expired-before-write).
9. **Run the search backfill in prod** — after deploying Phase 10, hit `POST /api/admin/backfill-search` once as an owner (idempotent) so existing cards are searchable by company/name and have a `category`. New cards populate on publish/edit automatically; only pre-Phase-10 rows need it. Confirm whether this has been run against production.
10. **Files follow-ups (optional):** switch **Load more** to **Next/Prev** if lists routinely reach the thousands (caps DOM, still keyset); **FTS5** contact-content search if an org exceeds ~10k cards; remove the temporary `POST /api/admin/backfill-search` route once every org is backfilled.
11. **Signature QR quiet-zone (optional polish, not yet done):** the QR is generated with `margin: 1` in `signature/[id]/page.tsx` (both the PNG and SVG fallback) and sits hard against the red divider. QR spec recommends a **quiet zone of 4**; bump `margin` to ~4 (and add a little left gap from the divider) for more reliable scanning in print/email. Content is correct (encodes the public `.vcf` URL); this is scannability only.

## Testing / running

- **Local dev:** `npm run dev` (localhost:3000). On a **fresh** local DB, seed a dev owner: `GET /api/dev/seed` (dev-only) → `owner@americaworks.test` / `correct-horse-battery-staple-12`. **NOTE:** the current local D1 (`.wrangler/state/v3/d1`) is a **copy of prod** (same db id, real accounts incl. `jclark@americaworks.com`), so the seed account does NOT exist there — sign in with a real account instead. `next dev` and `wrangler d1 execute --local` share that same DB.
- **Local dev gotchas (cost real time this session):**
  - **Port must be 3000, not 3002.** If `next dev` binds `:3002` (an orphan holds `:3000`, gotcha #6), sign-in POSTs 403 because Better Auth's origin/CSRF check expects `APP_URL`=`localhost:3000`. Free 3000 and restart.
  - **Migrations apply to `--local` separately** from the test DB and from `--remote`. A missing column on the local DB 500s the page just like prod. After generating a migration, apply it to local: `wrangler d1 execute aw-file-storage --local --file migrations/<n>.sql`.
  - **Dev public URLs point at prod** unless overridden: set `PUBLIC_FILE_DOMAIN="localhost:3000"` in `.dev.vars` (Phase 12) so dev-made cards get working local `/c/<slug>` links (this also makes localhost the "public host" → no login gate, counting on). Comment it out to test the `www` gating.
  - **Reset a local password / disable local 2FA** (when the prod-copy hash is stale): hash with `better-auth/crypto` `hashPassword` (run a node script FROM the project dir so `better-auth` resolves) and `UPDATE account SET password=…`; disable 2FA with `UPDATE user SET two_factor_enabled=0` + `DELETE FROM two_factor WHERE user_id=…`.
- **Typecheck:** `npm run typecheck`. **Tests:** `npm test` (Vitest + workers pool, 5 isolation tests).
- **Prod build preflight:** `npx opennextjs-cloudflare build`.
- API flows were verified with `curl` against real R2/D1 throughout Phases 1–3; UI verified in-browser in Phase 4.
