import { UploadCenter } from "@/components/upload-center";

// Upload Center — the default landing (spec 0004). The interactive uploader is a
// client component; the shell/layout handles auth.
export default function UploadCenterPage() {
  return <UploadCenter />;
}
