import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default OpenNext-on-Cloudflare configuration. Incremental cache, tag cache,
// and queue adapters can be added here later (e.g. for ISR or Cloudflare Queues).
export default defineCloudflareConfig({});
